# Phase 0 — data layer only. App/runtime deps land in Phase 1.
# Ranges (not hard pins) so the stack resolves across Python 3.11–3.14 until the
# runtime Python version is fixed (VPS chosen in Phase 6). Lower bounds are the
# versions this schema was designed against.
alembic>=1.13.2,<2
SQLAlchemy>=2.0.31,<2.1
psycopg[binary]>=3.2.10,<3.4
pgvector>=0.3.2,<0.5
pydantic-settings>=2.3.4,<3

# Phase 1 — RAG MVP runtime
fastapi>=0.111,<1
uvicorn[standard]>=0.30,<1
sentence-transformers>=3,<4
google-genai>=0.3,<2
httpx>=0.27,<1

# Phase 3 — evaluation + MLOps
mlflow>=2.14,<3        # local file-store tracking (no server); eval params/metrics/artifacts
pyyaml>=6,<7           # eval dataset (eval/dataset.yaml)

# Phase 4 — MCP server + agentic actions
mcp>=1.2,<2            # Model Context Protocol server (FastMCP, stdio) exposing the agent tools

# Phase 6 — productionization + observability
prometheus-client>=0.20,<1   # /metrics exposition for self-hosted Prometheus
redis>=5,<6                  # optional Redis cache/rate-limit client (prod compose enables)

# dev/test
pytest>=8,<9
