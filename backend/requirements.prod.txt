# Production API/worker dependencies only.
#
# Keep eval, MCP, and test tooling in requirements.txt so the runtime image does
# not ship packages that are not used by FastAPI or the job worker.
alembic>=1.13.2,<2
SQLAlchemy>=2.0.31,<2.1
psycopg[binary]>=3.2.10,<3.4
pgvector>=0.3.2,<0.5
pydantic-settings>=2.3.4,<3

fastapi>=0.111,<1
uvicorn[standard]>=0.30,<1
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.12.0+cpu
sentence-transformers>=5.5,<6
transformers>=5.10.2,<6
google-genai>=0.3,<2
httpx>=0.27,<1

prometheus-client>=0.20,<1
redis>=5,<6
