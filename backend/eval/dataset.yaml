# Second Brain RAG eval set (ADR-0008).
# expected_docs are document titles (the first H1 of each corpus file). Retrieval is measured at
# document granularity. expected_keywords are substrings a faithful answer should contain (used
# for the real-LLM run; the deterministic `fake` driver scores ~0 keyword recall by design).
cases:
  - id: hnsw-recall
    question: How do I improve recall when tuning the HNSW vector index?
    expected_docs: ["HNSW index tuning"]
    expected_keywords: ["ef_construction"]
    expect_refusal: false

  - id: hnsw-latency
    question: Which HNSW setting trades query latency for recall at search time?
    expected_docs: ["HNSW index tuning"]
    expected_keywords: ["ef_search"]
    expect_refusal: false

  - id: rrf-how
    question: How does reciprocal rank fusion combine vector and keyword results?
    expected_docs: ["Reciprocal rank fusion"]
    expected_keywords: ["rank"]
    expect_refusal: false

  - id: rrf-k
    question: What smoothing constant does reciprocal rank fusion use?
    expected_docs: ["Reciprocal rank fusion"]
    expected_keywords: ["60"]
    expect_refusal: false

  - id: fts-how
    question: How does Postgres do keyword search over the chunks?
    expected_docs: ["Postgres full-text search"]
    expected_keywords: ["tsvector"]
    expect_refusal: false

  - id: fts-rank
    question: Which function ranks full-text search matches in Postgres?
    expected_docs: ["Postgres full-text search"]
    expected_keywords: ["ts_rank_cd"]
    expect_refusal: false

  - id: runtime-stack
    question: What runs the production stack, and why not Kubernetes?
    expected_docs: ["Docker Compose runtime"]
    expected_keywords: ["Compose"]
    expect_refusal: false

  - id: db-port
    question: What host port does the local database publish on?
    expected_docs: ["Docker Compose runtime"]
    expected_keywords: ["5433"]
    expect_refusal: false

  - id: api-endpoints
    question: What are the main backend API endpoints?
    expected_docs: ["FastAPI backend"]
    expected_keywords: ["/chat"]
    expect_refusal: false

  - id: api-llm-swap
    question: How can the language model be swapped to a private local mode?
    expected_docs: ["FastAPI backend"]
    expected_keywords: ["Ollama"]
    expect_refusal: false

  - id: embed-model
    question: What embedding model is used and how many dimensions do the vectors have?
    expected_docs: ["MiniLM embeddings"]
    expected_keywords: ["384"]
    expect_refusal: false

  - id: hybrid-combine
    question: How does hybrid search combine semantic and lexical retrieval?
    expected_docs: ["Reciprocal rank fusion", "Postgres full-text search"]
    expected_keywords: ["hybrid"]
    expect_refusal: false

  - id: capture-no-scrape
    question: Does the capture endpoint scrape pages on the server?
    expected_docs: ["Web capture flow"]
    expected_keywords: ["server-side scraping"]
    expect_refusal: false

  - id: capture-tags-search
    question: What happens to tags saved through the web capture flow?
    expected_docs: ["Web capture flow"]
    expected_keywords: ["filtered in search"]
    expect_refusal: false

  - id: feedback-review-confirmations
    question: What must a reviewer confirm before promoting feedback into an eval case?
    expected_docs: ["Feedback eval loop"]
    expected_keywords: ["expected documents"]
    expect_refusal: false

  - id: feedback-export-command
    question: How are reviewed eval cases exported after promotion?
    expected_docs: ["Feedback eval loop"]
    expected_keywords: ["app.eval.export_cases"]
    expect_refusal: false

  - id: agentic-default-off
    question: Is Agentic RAG enabled by default?
    expected_docs: ["Agentic RAG opt-in"]
    expected_keywords: ["disabled by default"]
    expect_refusal: false

  - id: agentic-streaming
    question: Why is streaming unavailable for agentic RAG requests?
    expected_docs: ["Agentic RAG opt-in"]
    expected_keywords: ["citation validation"]
    expect_refusal: false

  - id: governance-admin-token
    question: Which token is required for destructive data operations?
    expected_docs: ["Data governance controls"]
    expected_keywords: ["X-Second-Brain-Admin-Token"]
    expect_refusal: false

  - id: governance-retention-erasure
    question: Does retention purge delete searchable chunks?
    expected_docs: ["Data governance controls"]
    expected_keywords: ["source level"]
    expect_refusal: false

  - id: briefing-worker-command
    question: Which worker command drains queued briefing jobs?
    expected_docs: ["Briefing worker queue"]
    expected_keywords: ["app.jobs.worker"]
    expect_refusal: false

  - id: briefing-empty-window
    question: What happens when the briefing window has no new documents?
    expected_docs: ["Briefing worker queue"]
    expected_keywords: ["nothing new"]
    expect_refusal: false

  - id: mcp-default-tools
    question: Which MCP tools are available by default?
    expected_docs: ["MCP local tools"]
    expected_keywords: ["search_notes"]
    expect_refusal: false

  - id: mcp-mutation-opt-in
    question: Which flag enables MCP tools that write tasks or research notes?
    expected_docs: ["MCP local tools"]
    expected_keywords: ["SECOND_BRAIN_MCP_ENABLE_MUTATIONS"]
    expect_refusal: false

  - id: upload-file-types
    question: Which file types can the multipart upload endpoint ingest?
    expected_docs: ["Multipart upload ingest"]
    expected_keywords: [".pdf"]
    expect_refusal: false

  - id: upload-binary-retention
    question: Are uploaded PDF binaries retained by default?
    expected_docs: ["Multipart upload ingest"]
    expected_keywords: ["not retained"]
    expect_refusal: false

  - id: status-authenticated-details
    question: What does the authenticated local status endpoint report?
    expected_docs: ["Local status panel"]
    expected_keywords: ["migration"]
    expect_refusal: false

  - id: status-worker-heartbeat
    question: Is the status panel using a separate worker heartbeat process?
    expected_docs: ["Local status panel"]
    expected_keywords: ["not a separate"]
    expect_refusal: false

  - id: off-corpus-dentist
    question: When is my dentist appointment next week?
    expected_docs: []
    expected_keywords: []
    expect_refusal: true

  - id: off-corpus-flight
    question: What time is my flight to Hanoi tomorrow?
    expected_docs: []
    expected_keywords: []
    expect_refusal: true

  - id: weak-context-meeting
    question: What did I promise Alex in yesterday's meeting?
    expected_docs: []
    expected_keywords: []
    expect_refusal: true
